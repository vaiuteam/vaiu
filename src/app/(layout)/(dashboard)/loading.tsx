import { DashboardSegmentSkeleton } from "@/components/loading-skeletons";

/** Navbar from dashboard layout persists; skeleton fills the routed content area. */
export default function Loading() {
  return <DashboardSegmentSkeleton />;
}
